cd frontend

npm install
npm install react-router-dom



1. Navbar is transparent on mobile version. i think you should make it have a white background that is ontop of the current screen and hides once clicked.
2. Also, after login, let the navbar show logout button instead.